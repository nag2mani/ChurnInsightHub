# TO Create virtual environment in Linux.
cd path/to/your/project
python3 -m venv virtualenviroment_name
source virtualenviroment_name/bin/activate


# To Install all package in requirment.txt
pip install -r requirements.txt
pip install flask
pip install flask-sqlalchemy


In terminal(to make db):
go to python environment
from app import db
db.create_all()
exit()


pip freeze > requirements.txt (run in a particular environment else it will consider global system).

